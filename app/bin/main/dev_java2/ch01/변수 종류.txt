//원시형 변수
chat, byte, short, int, long, float, double ; 정수형과 실수형, 크기, 범위, scope 다름
boolean ; 제어문에서 중요, 흐름을 다르게 할 때 사용, 분기문(if, switch)
호출 시 값이 나옴
원시형 변수에는 기본값이 있음 boolean 값이 맞으면 t, 틀리면 f
원시형 변수 초기화 시 대입 연산자 사용
변수의 사용 목적은 일괄처리
원시형 변수는 한 번에 하나의 값만 설정 가능 // 동일한 클래스 안에 동일한 타입 사용 불가능 << 새로운 값을 설정해 주면 덮어쓰기(새로운 객체 설정)

//참조형 변수
호출 시 주소번지 나옴