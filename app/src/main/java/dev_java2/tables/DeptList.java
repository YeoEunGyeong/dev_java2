package dev_java2.tables;

public class DeptList {
    // 부서 등록 ; 성공 0 실패
    int deptEnter(DeptVO pdVO) {
        int result = -1;
        return result;
    }

    // 부서 수정 ; 부서명과 지역만 수정 가능
    int deptCorre(DeptVO pdVO) {
        int result = -1;
        return result;
    }

    // 부서 삭제
    int deptDel(int deptno) {
        int result = -1;
        return result;
    }

    // 부서 정보 상세

    // 다 삭제 ^0^
    public static void main(String[] args) {

    }
}
